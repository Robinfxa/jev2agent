# 会话迁移便笺｜Semantic Reflex Runtime

> 基于截至 Codex 封包的本会话与交接材料整理。用于恢复项目背景，不新增架构条款、版本、批准或测试结果。
> 当前工程依据：`semantic-reflex-runtime-codex-handoff.zip`，交付标识 `codex-handoff-2026-09-18`。

## 1. 先知道我们停在哪里

**开放式成书已结束；下一步是 Codex 的 W1a 版本与原生能力核验，继而最小实现及 E0 真机验收，不是继续生成 v0.7。**

用户最后明确指定 Codex 为宿主并要求解决已登记问题、封包。包中的状态为 `SEALED_FOR_IMPLEMENTATION`，不是 `HOST_VERIFIED` 或 `DEPLOYMENT_APPROVED`。F1–F3 的设计路径已决定，真实实施证据仍 OPEN。

不要重新问用户选择什么宿主，也不要因为发现效果未知就重新开整本架构。真实事实推翻合同才登记受影响条款，做局部修订。

依据：handoff 的 `README.md`、`STATUS.json`、`ARCHITECTURE.md` B9、`docs/DECISION_CLOSURE.md`。

## 2. 用户意图与已确认选择

这些来自会话中的用户表达，不是从技术书稿反推的人格画像。

| 用户明确表达 | 应怎样延续 |
|---|---|
| 最初要求从材料提取项目，并反复进行 bull／bear 评估 | 支持和反对必须落到选择、代价和推翻条件；不要只替项目寻找合理化理由 |
| 希望参考 ValueHermes 架构书组织计划，并逐步细化 | 借用总书、模块、证据和收束纪律；不是绑定 ValueHermes/Hermes 的技术栈或业务 |
| 欢迎搜索 GitHub、Hacker News、Jev 社区和官方文档，带着新看法回来 | 有网络时用外部证据核验、复用或否决具体方案；不以链接数量替代判断 |
| 问“我们要一直从0.6丰满到9.99吗。有没有什么认定架构相对完备的指标” | 用户不希望无限成书；范围、关键合同、失败规则和验证方式清楚后应收束 |
| 说“指定codex最为宿主，然后解决你提到的问题，封包吧” | Codex 不再是候选宿主；推进有限实施，而不是重新横向选平台 |

**授权不能扩大解释：** 用户指定 Codex 并授权封包，不等于对全部工程细节逐条签字，也不等于授权读取账户凭据、任意付费调用、生产干预或部署。当前工程落点由交接包裁定；新增操作遵守新会话的实际授权。

## 3. 项目初心与首发范围不是一回事

长期关注：同一个 coding／multi-agent 工作流，能否借助恰当的信息披露和状态信号，以更少无效工作、更低真实成本完成同样可靠的任务。早期也关心 review、缺陷是否值得继续修、阶段是否完成。

现在只做 **一种只读搜索结果的 ADMIT＋原文 recall**。Jev 是可替换的语义后端候选，不是项目必须证明正确的前提，也没有被绑定为首发必装组件。

首发工程裁定（来自交接包，不是本轮官方事实核验）：

| 项目 | 当前决定 |
|---|---|
| 宿主形态 | Codex CLI，单任务非交互运行，一个授权域 |
| 接入 | 本地 stdio MCP provider，自有 `srr_search` 返回前处理；`srr_recall` 读取原文 |
| 覆盖 | 只覆盖实际经过自有搜索工具的返回；不禁用原生搜索、读取、编辑或测试来制造覆盖率 |
| 可信状态 | 可信启动器生成受保护 run manifest；模型参数不能提升权限或重写任务合同 |
| 存储 | 每 run 本地 SQLite 事务；源、视图和最低恢复记录提交后才发布引用 |
| 实施栈 | 包中选择 Python 3.11+ 与标准库 SQLite；MCP SDK/生产 wire/启动语法须按实际版本核验 |
| 默认开关 | `admission_mode=off`，`semantic_backend=disabled` |

首发不做生成式摘要、subagent 报告筛选、完整 EXPAND、DISCLOSE、OBSERVE、review/repair/completion、逐动作审批、通用事件总线、全库语义索引或多宿主平台。后置不是永久否定，但不能自动前移。

依据：`ARCHITECTURE.md` B0–B1、`docs/CODEX_HOST_CONTRACT.md`、`docs/IMPLEMENTATION.md`。

## 4. 最值得保留的“为什么”

| 当前取舍 | 保留它的理由与边界 |
|---|---|
| 从每步监督收窄到信息首次进入前 | 不重复主模型刚基于相同信息完成的决策；新层必须避免或替代真实成本 |
| 不依赖全局 hook | 默认只处理自有工具的返回边界，避免把“能收到日志”当成“能控制下一次模型输入” |
| 原生与确定性机制优先 | `Harness-native → Static instructions → Deterministic policies → Semantic Reflex`；先建立合理 A 和强 B |
| Hard Context 不是语义打分产物 | 必须由当时真实可得的任务／权限合同声明；不能根据隐藏答案补必读内容 |
| 可召回不等于安全 | agent 可能不知道遗漏，原文恢复也不能撤销此前错误行动；证据未进入捕获时，换 selector 无法补出事实 |
| 不以首次压缩证明收益 | 目录、恢复、重复阅读、缓存、失败、人工和维护可能反转表面优势；本地字节不是实际 token 或账单 |
| 同时比较 B/A、C/B、C/A | B 是确定性整理＋恢复，C 在共同基础上加语义选择。C 赢 B 仍可能整包输 A |
| 允许删 C 或停项目 | 规则／读取机制有价值时可以只保留 B；语义层没有自动存在权，代码沉没成本也不是保留理由 |

还需特别记住：事件级无效／超时与合法块级弃权不同；后者先保留并求结构闭包，超预算不能又回到会延后该块的 B。发送成功不等于模型收到，未知交付不盲目补发。停新筛选不等于关闭仍合法的旧原文召回。

上述是现行合同的摘要，细节以 `ARCHITECTURE.md` B3–B8 为准，不另外定义第二套规则。

## 5. 证据状态：哪些绝不能在新会话被误记为通过

| 对象 | 截至封包的状态 |
|---|---|
| Codex 选择 | 用户已指定 |
| CLI/MCP/SQLite 工程路线 | 已确定实现方向，未在目标版本核验 |
| 生产 provider | 包中没有实现；不存在现成 `srr-mcp` 命令 |
| Codex 真机集成 | 未执行；封包环境的 PATH 未发现 Codex |
| 官方资料／社区网页 | 封包阶段没有联网核验；早期材料的“当前”“可安装”“很便宜”均只能作待查线索 |
| F1–F3、E0 | 方案决定，运行证据开放；E0 未执行／模板 NOT_READY |
| E1–E5、语义增益、完整任务经济性 | 未验证；生产部署未批准 |
| 封包辅助检查 | 原 VALIDATION 记载 23 项有限测试通过；仅支持其注明的检查范围 |
| 历史实验 | 本地构造、脚本需求和有限合同测试；不是自然 agent 成功率或可靠性证明 |

本便笺没有重跑旧测试、调用 Codex 或验证真实费用。历史数字只按各自报告引用，不累计成新的“通过数量”。新会话应重新检查自己的环境；旧环境没有 Codex／网络，不代表新环境也没有。

依据：`STATUS.json`、`VALIDATION.md`、`docs/EVIDENCE_AND_LIMITS.md`。

## 6. 下一轮应该产出什么

先读 `README.md`、`ARCHITECTURE.md`、`docs/CODEX_HOST_CONTRACT.md`、`docs/E0_CODEX_ACCEPTANCE.md`、`docs/IMPLEMENTATION.md`，再按 `CODEX_HANDOFF.md` 执行。

**第一份有价值的反馈是 W1a：** 实际 Codex 二进制／版本与来源、MCP 配置依据、合法模型请求观察点、隔离方案，以及哪个已登记条款能够或不能成立。先在授权本机使用包中的探测工具；读取二进制帮助须显式选择相应参数，不读取凭据。

然后才是 W1b/W1c：隔离 smoke 工作区、全量只读搜索与原文 reader、可信 manifest、事务与分页、E0 H01–H14。F1 必须证明真实前置边界／交付；F2 必须证明可信任务与权限／信息隔离；F3 必须证明原文、重启、压缩、撤权和停筛恢复。

若新环境缺少二进制、网络或合法观察点，就提交具体 NOT_RUN／FAIL、受影响条款和最小所需证据，保持干预关闭。不拿模拟 trace 代真机，也不因此重新打开全书。

在线研究只围绕当前阻塞：官方文档、实际版本源码与测试为接口依据；Issues/社区可揭示失败，需追查原始证据。记录版本、日期和核验范围；不得从旧稿复制“最新版本”或预期价格。

汇报保持包中的格式：实际版本与来源；实现差异；E0 通过／失败／未执行；费用与副作用；阻塞条款及最小修复。

## 7. 材料权威与隔离

新会话的用户明确要求可以改变目标；变更应显式记录。现行规范是 handoff 的 `ARCHITECTURE.md`、Codex 宿主合同和 E0 验证义务；`STATUS.json`/`VALIDATION.md` 表明实际进度；本便笺只恢复背景。历史版本、早期研究和便笺不能覆盖现行合同。发现现行冲突时登记，不从历史中挑更宽松的规则。

handoff 已含 v0.6 与 closeout 两个原档案、历轮论证／代码／公开测试及部分参考材料，通常无需重新上传 v0.1–v0.5。v0.6 的讨论来源含节选，ValueHermes 仅有选用副本；这不等于完整逐字会话或完整原始 `docs.zip`。需要保存全部原始参考时，另存用户原 `docs.zip`，但不作为下一轮必读资料。

**实施助理查历史，与受测 agent 得到资料，是两种权限。** archive 含 oracle/验收答案，不能整个挂到受测工作区；公开反例只能开发回归，不能冒充新留出集。

## 8. 删除会话前的最小留存

下载迁移包到自己可控制的存储，新会话重新上传，不把旧 sandbox 链接或自动记忆当唯一备份。迁移包只把原 handoff 与本便笺、启动消息放在一起；原 handoff 字节不变。

原 handoff SHA-256：`7387c1e505f005890133455d7892c789679e8ff6261d3fd777643f2c79aecdcd`。

这份摘要不是完整聊天导出，也不包含未提供的仓库地址、账户配置、Codex 版本或秘密。下一轮确实需要这些事实时，从授权环境取得，不能自行补全。
