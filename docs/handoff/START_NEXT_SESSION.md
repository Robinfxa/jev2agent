# 新会话启动消息

把迁移包上传后，可以粘贴下面的消息。它是工作入口，不是部署批准。

---

这是 Semantic Reflex Runtime 项目的续接会话。请先读附件中的 SESSION_CONTEXT.md，再打开原 Codex handoff，按 CODEX_HANDOFF.md 指定顺序读取现行文件与 STATUS.json、VALIDATION.md。

宿主已明确指定 Codex；架构成书已收束，不要再问选哪个宿主，也不要默认生成 v0.7 或扩大范围。首发仅 Codex CLI 单任务下，经本地 stdio MCP 的 srr_search 返回前处理与 srr_recall；具体版本接口仍需核验。

包是架构实施交接，不是已安装插件：provider 尚未实现，E0 未通过，admission=off，语义后端 disabled。请不要把历史本地测试当真实 Codex 结果。

现在从 W1a 开始：在授权环境核验实际 Codex 版本、MCP 适配、合法请求观察点和隔离。网络可用时查当前官方文档及对应版本源码；没有条件就报告具体缺失，保持 off，不造 trace 或安装命令。不得读取登录凭据或未经授权启动付费／生产任务。

输出实际证据、通过／失败／未执行项、对应合同和最小下一步。仅当证据推翻既有条款时提出局部修订。早期 archive 含验收答案，不得直接挂载给受测模型。
