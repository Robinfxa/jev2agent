# 本次来源与证据边界

## 已提供材料

本包以现有结项评审、冻结候选和 v0.6 为基础；不重新引入早期“review首发”“四能力同时首发”等已收窄方案。原稿的架构词汇与 A/B/C、E0–E5、F1–F3 保持一致。

[references/closeout](../references/closeout/README.md) 保存结项包原件和带行号副本；[archive](../archive/semantic-reflex-runtime-book-v0.6.zip) 原封保存历史书稿、源码、测试和诊断。`SOURCE_MANIFEST.json` 记录 SHA-256。历史测试数字只是原报告，除非本次 VALIDATION 明确写重跑，不当作本次执行结果。

## 本轮工程裁定

用户新指定宿主 Codex。CLI单任务/一个授权域、本地stdio MCP、自有 literal 搜索工具、可信run manifest、每run SQLite及具体恢复操作，是本轮为 F1–F3 选择的实现路径，不伪装成原材料已经定稿的方案。

这些选择来自职责、风险和故障的工程推理，不是对 Codex 最新文档或接口的知识背书。是否能以目标版本实际做到，要现场查明；自有工具边界减少对未知全局 hook 的依赖，但不能替代 E0。

## 本轮实际环境

网页搜索不可用；无网络资料检索。`evidence/host-probe.json` 记录本轮本机定位结果。没有查用户账户、没有读取登录凭据、没有调用 Codex 模型或语义后端。

版本、模型、MCP SDK、原生参数、请求可观测接口、权限机制、使用费用都没有通过当前官方来源核验。没有把外部项目的旧讨论或供应商说法升级为事实。

## 自动检查的边界

文件hash、链接、JSON和结构测试只能检查材料一致性。`check_e0_evidence.py` 不能判断证据真实性、判断模型是否实际使用内容、完成独立安全审计或批准上线。提供有文件hash的证据也仍需人审。

本包没有 provider实现、生产鉴权器、崩溃持久性证明或真实Codex 实验。设计已封包不是产品已部署；不存在后台工作或以后自动补测。
