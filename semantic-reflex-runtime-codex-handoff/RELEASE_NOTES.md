# 封包说明｜codex-handoff-2026-09-18

本次不是增加一个新的架构书版本，而是把结项候选绑定到用户指定的 Codex，形成一次实施交接。

## 已完成的文档与设计工作

宿主家族 Codex 已指定。工程形态选择 CLI 单任务非交互；接入采用自有 stdio MCP 搜索/召回；权限由可信 run manifest 约束；源与视图采用每 run 的 SQLite 事务；交付不明停止而不盲重发。三处旧正文冲突已在现行基线整合。42 项建议、18 项未决和6项研究事项全部分流，无需继续逐章续写。

## 没有被假装完成的事情

当前没有 Codex 可执行文件或最新文档核验；未提供生产 provider，未跑真实模型链路，未证明 OS 隔离、存储崩溃恢复、语义质量和任务净收益。F1–F3 的设计解法明确，现场证据仍开放，E0 模板仍 NOT_RUN。用户指定产品不等于某一具体二进制版本已通过。

## 资料结构

`ARCHITECTURE.md` 是现行逻辑基线，`docs/CODEX_HOST_CONTRACT.md` 是具体映射，`docs/E0_CODEX_ACCEPTANCE.md` 是验收合同。`CODEX_HANDOFF.md` 供实施者接手。`config/` 为本项目的意图与证据模板，不是 Codex 原生配置。`tools/` 只做本地探测、资料完整性和文件校验。

`references/closeout` 保留结项来源；`archive` 原封保存 v0.6 和结项 ZIP。历史内容不与当前正文争夺规范权，公开反例不作为未来留出数据。不要将整个档案暴露给受测模型。

## 后续变更规则

实现中发现具体接口、权限、故障或源版本合同不成立，记录问题并只重开相应条款；普通实现补丁、数据更新和实验结果不升级整册架构。启用必须另经现场验收与实验授权，不因封包而默认打开 B 或 C。
