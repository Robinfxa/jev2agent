--- a/docs/architecture/modules/17-scope-and-activation.md
+++ b/docs/architecture/modules/17-scope-and-activation.md
@@ -39,7 +39,7 @@
 
 ## 5. 成书与实施状态
 
-技术章可以成为完整 Draft，但没有用户逐项评审、真实接入和实验结果，不能标 Accepted 或 Verified。本轮新增可执行合同实验是设计自检，不是正式宿主 Runtime 实现。
+设计状态与验证状态分开登记。Draft 是草案；Reviewed 说明明确范围的评审已执行，并注明评审者及其独立性；Accepted 仅表示项目负责人已批准该范围的设计基线，不表示实现或收益通过。Verified 必须绑定具体对象、宿主/配置版本、测试范围和实际证据。宿主无关合同可先单独提交设计批准；首个宿主的可行性阻塞仍保持开放，未核验前不得标记适配方案完成，也不得据此宣布整个首发架构阶段归档。本地合同实验仅为设计自检，不构成宿主或效果验证。
 
 ## 8. v0.3：架构扩张的暂停条件
 
--- a/docs/architecture/modules/16-first-experiment.md
+++ b/docs/architecture/modules/16-first-experiment.md
@@ -93,7 +93,7 @@
 
 W0 增加离线描述分析器，42 项合同/算术测试与两个手工 CLI 反例实际执行；v0.2 的 71 项有限模型及 v0.1 的 11 项样例均已回归。它们不覆盖 E0–E5，也不决定生产语言。
 
-实施交接见 [v0.3 交接单](../../plans/implementation-handoff-v0.3.md)。本轮推荐从继续扩张版图转向真实接缝/数据或具体合同问题；四能力首发范围不变，仍无全面实施授权。
+实施交接见 [v0.3 交接单](../../plans/implementation-handoff-v0.3.md)。本轮推荐从继续扩张版图转向真实接缝/数据或具体合同问题；四能力长期版图不变，首发仅一种只读搜索结果的 ADMIT＋原文 recall，仍无全面实施授权。
 
 ## 12. v0.4：当前有可执行候选，不是实验晋升
 
--- a/docs/architecture/modules/05-semantic-kernel.md
+++ b/docs/architecture/modules/05-semantic-kernel.md
@@ -44,7 +44,7 @@
 
 ## 7. v0.2：有限输出、跨块依赖与注入边界
 
-单块判断只能表达它在给定信息下的优先级，不能证明删除其他块不会影响联合解释。定义、例外、调用位置跨块关联时，优先保留确定性结构组；结构关联未知或组超预算时回基线，不偷偷只传一半证据。
+单块判断只能表达它在给定信息下的优先级，不能证明延后其他块不会影响联合解释。定义、例外、调用位置跨块关联时，优先保留确定性结构组；具体弃权、结构闭包和溢出退出统一按 §06.11。事件级不可用可回允许的 B/A；合法块级弃权先保留及求闭包，保护/弃权闭包超预算不能回到会延后该证据的 B。结构关联无法可靠确定时，不把未知当独立，也不生成无法证明结构完整的延后信封；使用满足原约束的原生完整读取或明确分页路径，否则诚实退出。
 
 输入中的代码、注释、文档和工具正文都属于待判断数据，不得决定系统策略或授权字段。后端返回先经过 schema、枚举、输入引用和版本校验；额外字段、非法分数或伪造 block ID 被视为无效输出。
 
